---
status: completed
created_at: 2026-09-25
updated_at: 2026-09-25
files_edited:
  - AGENTS.md
  - README.md
  - cmd/AGENTS.md
  - docs/AGENTS.md
  - docs/README.md
  - docs/agent-docs.md
  - docs/architecture.md
  - docs/cli.md
  - docs/configuration-live-matrix.md
  - docs/configuration.md
  - docs/extensions.md
  - docs/getting-started.md
  - docs/harness-compatibility.md
  - docs/integrations.md
  - docs/persistent-instructions.md
  - docs/programmatic-integration.md
  - docs/provider-canary-threat-model.md
  - docs/tasks-and-goals.md
  - docs/troubleshooting.md
  - docs/vision.md
  - internal/AGENTS.md
  - internal/agentdocs/AGENTS.md
  - internal/agentdocs/agentdocs.go
  - internal/agentdocs/agentdocs_test.go
  - internal/agentdocs/content.go
  - internal/agentdocs/prompt.go
  - internal/app/AGENTS.md
  - internal/app/cleanup.go
  - internal/app/cleanup_test.go
  - internal/app/command_menu.go
  - internal/app/command_menu_test.go
  - internal/app/configuration.go
  - internal/app/job_archive.go
  - internal/app/job_archive_test.go
  - internal/app/job_info.go
  - internal/app/job_info_test.go
  - internal/app/job_reconnect_test.go
  - internal/app/message_admission_test.go
  - internal/app/prompt_context_test.go
  - internal/app/recovery.go
  - internal/app/recovery_test.go
  - internal/app/runtime_log_test.go
  - internal/app/runtime_test.go
  - internal/app/service.go
  - internal/app/service_test.go
  - internal/app/session_name_test.go
  - internal/app/work_list.go
  - internal/app/work_list_test.go
  - internal/channel/channel.go
  - internal/channel/telegram/AGENTS.md
  - internal/channel/tui/AGENTS.md
  - internal/channel/tui/logo_activity_test.go
  - internal/channel/tui/notification_test.go
  - internal/channel/tui/tui.go
  - internal/channel/tui/tui_test.go
  - internal/channel/tui/visual_capture_test.go
  - internal/channel/whatsapp/AGENTS.md
  - internal/cli/AGENTS.md
  - internal/cli/cli.go
  - internal/cli/cli_test.go
  - internal/cli/docs_test.go
  - internal/cli/integration_test.go
  - internal/cli/notification_watch.go
  - internal/cli/notification_watch_test.go
  - internal/cli/plain.go
  - internal/cli/server_runtime.go
  - internal/cli/server_runtime_test.go
  - internal/config/AGENTS.md
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/config/settings.go
  - internal/config/settings_test.go
  - internal/core/types.go
  - internal/extensions/AGENTS.md
  - internal/extensions/hooks.go
  - internal/extensions/hooks_test.go
  - internal/harness/AGENTS.md
  - internal/harness/harness.go
  - internal/harness/pi.go
  - internal/harness/process_fixture_test.go
  - internal/harness/supervisor.go
  - internal/history/AGENTS.md
  - internal/history/store.go
  - internal/history/store_test.go
  - internal/instructions/AGENTS.md
  - internal/instructions/instructions.go
  - internal/instructions/instructions_test.go
  - internal/localapi/AGENTS.md
  - internal/localapi/client.go
  - internal/localapi/job_activity_test.go
  - internal/localapi/localapi_test.go
  - internal/localapi/server.go
  - internal/orchestrator/AGENTS.md
  - internal/orchestrator/agentdocs_prompt_test.go
  - internal/orchestrator/create.go
  - internal/orchestrator/durable_timing_test.go
  - internal/orchestrator/manager.go
  - internal/orchestrator/manager_test.go
  - internal/orchestrator/markdown.go
  - internal/orchestrator/notification.go
  - internal/orchestrator/notification_decision.go
  - internal/orchestrator/notification_decision_test.go
  - internal/orchestrator/notification_message.go
  - internal/orchestrator/notification_message_test.go
  - internal/orchestrator/notification_test.go
  - internal/orchestrator/ordinary_agent.go
  - internal/orchestrator/provider_lock_unix.go
  - internal/orchestrator/provider_lock_windows.go
  - internal/orchestrator/retention.go
  - internal/orchestrator/review_mode.go
  - internal/orchestrator/review_policy_test.go
  - internal/orchestrator/routes.go
  - internal/orchestrator/routes_test.go
  - internal/orchestrator/semantic_heartbeat.go
  - internal/orchestrator/semantic_heartbeat_test.go
  - internal/orchestrator/status.go
  - internal/orchestrator/status_test.go
  - internal/orchestrator/work_list.go
  - internal/orchestrator/work_list_test.go
  - internal/orchestrator/workflow.go
  - internal/orchestrator/workflow_test.go
  - internal/workspace/AGENTS.md
  - internal/workspace/templates/AGENTS.md
  - internal/workspace/templates/agent-chat.md
  - internal/workspace/templates/agent-developer.md
  - internal/workspace/templates/agent-heartbeat.md
  - internal/workspace/templates/agent-notification.md
  - internal/workspace/templates/agent-reviewer.md
  - internal/workspace/templates/chat.md
  - internal/workspace/templates/config.yaml
  - internal/workspace/templates/create-goal.md
  - internal/workspace/templates/create-task.md
  - internal/workspace/templates/extensions.md
  - internal/workspace/templates/goal-review.md
  - internal/workspace/templates/goal.md
  - internal/workspace/templates/goals-AGENTS.md
  - internal/workspace/templates/heartbeat.md
  - internal/workspace/templates/notification.md
  - internal/workspace/templates/recovery.md
  - internal/workspace/templates/review.md
  - internal/workspace/templates/task.md
  - internal/workspace/templates/tasks-AGENTS.md
  - internal/workspace/templates/workspace-AGENTS.md
  - internal/workspace/workspace.go
  - internal/workspace/workspace_test.go
  - npm/test.js
  - package.json
  - scripts/AGENTS.md
  - scripts/smoke.sh
  - internal/agentdocs/catalog_sync_test.go
  - internal/app/correlation.go
  - internal/app/maintenance.go
  - internal/app/notification.go
  - internal/app/notification_test.go
  - internal/app/retired_workflow_test.go
  - internal/harness/native_input_test.go
  - internal/harness/pi_native_command_negative_test.go
  - internal/harness/pi_telegram_note.go
  - internal/harness/pi_telegram_note.js
  - internal/harness/pi_telegram_note_identity_unix.go
  - internal/harness/pi_telegram_note_identity_unix_test.go
  - internal/harness/pi_telegram_note_identity_windows.go
  - internal/harness/pi_telegram_note_identity_windows_test.go
  - internal/harness/pi_telegram_note_smoke_test.go
  - internal/harness/pi_telegram_note_test.go
rationale: Retire the workspace workflow and orchestrator layer, route ordinary Pi chat directly to the provider with the raw accepted user message and deterministic framework commands, keep the bounded-history fallback for every other harness, and add one additive Telegram-only Pi system-prompt note through a trust-validated private runtime extension. The follow-up work published that rework as the breaking v2.0.0 release through the documented two-stage release process and deployed it to the local npm-managed home service.
supporting_docs:
  - ../../../docs/agent-docs.md
  - ../../../docs/architecture.md
  - ../../../docs/cli.md
  - ../../../docs/configuration.md
  - ../../../docs/extensions.md
  - ../../../docs/harness-compatibility.md
  - ../../../docs/integrations.md
  - ../../../docs/programmatic-integration.md
  - ../../../docs/provider-canary-threat-model.md
  - ../../../docs/releasing.md
  - ../research/2026-09-19-spynel-architecture-security-quality-gates.md
  - 2026-09-19-digitalygo-repository-migration.md
  - 2026-09-19-local-pi-configuration.md
  - 2026-09-20-pi-session-controls.md
  - 2026-09-20-telegram-topic-rich-text.md
  - 2026-09-21-pi-retained-prompt-context.md
  - 2026-09-21-pi-session-topic-naming.md
  - 2026-09-22-elevenlabs-speech-transcription.md
  - 2026-09-22-job-archive-shutdown-quiesce.md
  - 2026-09-22-pi-rate-limit-answer-recovery.md
  - 2026-09-23-telegram-command-menu-and-stale-thresholds.md
  - 2026-09-23-transcript-echo.md
  - https://github.com/digitalygo/spynel/releases/tag/v2.0.0
  - https://github.com/digitalygo/spynel/actions/runs/36162044479
  - https://github.com/digitalygo/spynel/actions/runs/36163619933
---

# Pi-direct chat rework and workflow retirement

## Summary of changes

This operation records the completed, user-authorized Pi-direct rework on top of `8425af2` on `main`. The delta retires the workspace task/goal workflow layer and routes ordinary chat prompts directly to the executing harness: the `internal/orchestrator` and `internal/instructions` packages, task/goal/review/semantic-heartbeat/notification-agent/automatic-recovery behaviors, the workflow configuration keys and templates they depended on, and their commands, API surface, and documentation were removed. At the same time, Pi ordinary chat now receives the raw accepted current user message with no Spynel persona, no durable history path, and no bounded history seed; unknown slash input reaches Pi's native prompt pipeline while known Spynel commands stay deterministic; and one embedded Pi extension loaded additively with `--extension` supplies the Telegram-only final-reply note.

The delta spans 165 paths: 149 tracked files changed or deleted and 16 new files under `internal/`. Existing workspace documents, configuration, histories, and credentials were left untouched and inert. The work is uncommitted on top of the baseline and is not part of the published `v1.6.0` release.

## Technical reasoning

### Workflow layer retirement

The workflow layer duplicated coordination intelligence that belongs to harness sessions. Spynel is a classic, non-AI program that coordinates and oversees external harnesses; it should translate channel messages into provider-neutral dispatches and own transport, session, and durable runtime state rather than host its own task/goal planning, review, heartbeat, and notification agents. Removing `internal/orchestrator` and `internal/instructions` also removed the provider-lock, retention, review-policy, route, status, work-list, and workflow code that only served those behaviors.

Workspace data was treated as historical. The implementation never scans, rewrites, or deletes existing task/goal files, prompts, configuration, or credentials, and the new `internal/app/retired_workflow_test.go` pins sentinel files as unwritten and unremoved with unchanged permissions. The retired `harness.reviews` and `harness.*_agent_prefix` keys are ignored on load like any unused key and disappear on the next canonical save, which follows the no-migration policy: one current representation, no fallback readers.

### Direct Pi prompts

Ordinary chat dispatch no longer injects persona, workflow, documentation, scope-discipline, or honesty boilerplate. Pi receives the raw accepted current user message byte-for-byte on both fresh and retained sessions, with no bounded Spynel history seed and no durable history path. The optional provider-neutral input capability keeps `internal/app` harness-neutral, so only Pi changes behavior. A known Spynel slash command still routes deterministically through the shared application service and still refuses when the surface or authorization disallows it, while unrecognized slash input is passed through as raw text so Pi can expand its own skills, prompt templates, and extension commands through the ordinary prompt pipeline.

Every other harness and every non-native session key keeps the provider-neutral bounded history context. Its prior-history seed follows the existing `ConversationContextProvider` capability, and absence, uncertainty, or any other safe-false answer keeps the bounded seed. The current user message is delivered in every chat prompt, including when history limits are zero.

### Telegram Pi note

The note is one embedded, dependency-free Pi extension materialized into the private workspace runtime directory and passed with a single additive `--extension` argument for `chat:telegram:` Pi RPC sessions only. Its `before_agent_start` handler writes exactly one namespaced `spynel_telegram` section into the structured system prompt: the user sees final replies only, so any clarification or approval request must be one final question that ends the turn, and the next user reply continues the same Pi session with no artificial pause state.

Additive loading is the deliberate choice. `--append-system-prompt` was rejected because it suppressed Pi's trusted and global `APPEND_SYSTEM.md` discovery. A real Pi run with the fake provider verified that the additive extension path preserves that discovery parity while still delivering the note, and Pi keeps loading global extensions, skills, prompt templates, themes, context files, and model/provider settings, with project-local resource loading left to Pi's own standard non-interactive trust decision. The note is guidance, not enforcement: a user extension that returns a forced prompt can override it, extension-triggered model runs may bypass `before_agent_start`, unsupported interactive extension dialogs are cancelled rather than left hanging, and Spynel claims no model-compliance guarantee.

### Extension trust boundary

The materialized extension file lives inside the workspace `.spynel/runtime` tree. Before any launch, the state root and the runtime directory beneath it are each validated by `Lstat` as real, current-user-owned directories with no group or other permission bits; a missing directory is created `0700`, and a pre-existing directory that violates that private trust contract fails the launch closed instead of being chmodded or replaced. An existing extension file is reused only when it is a regular non-symlink file with exactly the embedded bytes, private permissions, and current-user ownership. Anything else is repaired through a `0600` temporary file and one atomic rename that never follows the target, concurrent launches materialize identical bytes, and a directory occupying the target path fails the launch instead of being deleted. This closes the security review finding about launching a Pi extension from a directory the user does not exclusively control.

### Preserved behavior

The TUI, Telegram, WhatsApp, and plain CLI channels stay, as do the other harnesses. The authenticated primary election and loopback service boundary, ordinary job and status surfaces, explicit `spynel notify` outbox delivery with its 30-second pending retry through the primary maintenance loop, the existing eight-hour automatic history and job-archive cleanup, speech transcription and media handling, and the Pi session controls (`/pi session`, `/pi compact`, `/pi import`, `/pi name`) are unchanged. Conversation identity, session naming, Telegram topic routing, rich-text chunking, transcript echo, and rate-limit answer recovery are untouched.

### Legacy sessions and residual effects

Legacy retained Pi sessions may still contain old dispatcher messages from before the rework, because Spynel does not rewrite provider session files. `/clear` deletes that conversation's durable history and resets its provider session if the user chooses. The retired workspace documents remain inert on disk.

## Impact assessment

- The product model is now enforced in code: the elected owner's background loop only retries pending explicit outbox deliveries and runs history and job cleanup, and no orchestrator, semantic heartbeat, notification agent, or autonomous unread-message scan exists.
- Ordinary Pi conversations receive exactly the accepted user text. Other harnesses and every non-native session key keep the bounded history seed and its safe-false capability rules, so their behavior does not regress.
- Known framework commands remain deterministic across channels, while unrecognized slash input no longer stops at Spynel and can reach Pi's own command expansion.
- Telegram Pi sessions carry one final-reply guidance note loaded additively. Because it is guidance only, user extensions and extension-triggered runs can override or bypass it without changing Spynel behavior.
- Existing workspace task/goal documents, prompts, histories, and credentials remain on disk and inert. Retired configuration keys are ignored on load and removed by the next canonical save, with no migration and no rewriting of untouched files.
- The repository structure shrank by two packages and their DOX files. `docs/persistent-instructions.md` and `docs/tasks-and-goals.md` were removed, the root and package `AGENTS.md` contracts were updated, and the new `internal/agentdocs/catalog_sync_test.go` keeps the static documentation command list and the canonical slash catalog in lockstep in both directions.
- Documentation for architecture, harness compatibility, integrations, configuration, extensions, CLI, programmatic integration, and the provider canary threat model was synchronized with the new boundaries and the extension trust validation.
- No commit, push, release, or deployment was performed. Nothing in this record claims that the rework is available to installed users.

## Validation steps

- Baseline: at task start `HEAD` was `8425af2` on `main` with a clean tracked tree. The only untracked, non-ignored path predating the task was the private `.ai-telemetry/` directory, which was never inspected, staged, modified, or packaged.
- Because that untracked directory makes the repository-wide DOX and smoke gates fail for reasons unrelated to the change, the exact delta was copied into a clean detached Git worktree and all repository-wide validation ran there.
- Repository gates passed: `scripts/dev.sh dox` reported DOX coverage valid for 49 tracked directories, and `scripts/dev.sh test`, `go test ./...`, `go test -p1 -count1 ./...`, `go vet ./...`, a Go build of `./cmd/spynel`, `scripts/smoke.sh`, `node npm/test.js`, and `git diff --check` all completed without failures.
- Packaging: `npm pack --dry-run` listed nine files and no telemetry artifacts.
- Targeted race runs passed for `internal/app`, `internal/harness`, `internal/localapi`, `internal/workspace`, and `internal/config`. The TUI race run passed with the strict p95 latency threshold excluded under the race detector, the real TUI PTY race test passed, and the replaced Bubble Tea module's nested race tests passed. The normal p95 latency test passes in regular `go test`.
- New-behavior coverage: `internal/app` at 83.7%, notification-origin normalization at 100%, `Process` and `Enqueue` above 95%, Pi slash handling at 100%, private extension directory validation at 83%, and the extension materializer at 75% because the remaining OS I/O failure arms are not injectable.
- The hybrid quality judgment passed twice. The security review finished at full PASS after two blocked findings were fixed and re-reviewed: an untrusted Pi extension directory, fixed by the private runtime ownership and mode validation described above, and a malformed outbox `Attempts` panic, fixed and followed by a `MaxInt-1` repeated-delivery advisory fix.
- An unrelated pre-existing test data race in `internal/app/runtime_log_test.go` was fixed without a production change.

## Related operation traces

No earlier operation record covers this decision. The records below remain historical and are cross-linked, not rewritten.

- [Pi retained prompt context](2026-09-21-pi-retained-prompt-context.md) recorded the bounded-seed and current-message rules that this rework replaces for Pi with the raw-message boundary; its non-Pi seed rules remain.
- [Pi session controls](2026-09-20-pi-session-controls.md) owns the `/pi` control surface kept here.
- [Pi session and Telegram topic naming](2026-09-21-pi-session-topic-naming.md) owns the session naming and topic rename behavior kept here.
- [Telegram topic conversations and bounded rich-text replies](2026-09-20-telegram-topic-rich-text.md) owns the canonical topic conversations, rich-text chunking, and rate-limit retry kept here.
- [Pi rate-limit answer recovery](2026-09-22-pi-rate-limit-answer-recovery.md) owns the recovery path kept here.
- [Telegram command menu and stale thresholds](2026-09-23-telegram-command-menu-and-stale-thresholds.md) owns the command menu and readiness thresholds; the rework removed the retired workflow commands from the shared catalog.
- [Transcript echo](2026-09-23-transcript-echo.md) and [ElevenLabs speech transcription](2026-09-22-elevenlabs-speech-transcription.md) own the speech and transcript behavior kept here.
- [Job archive shutdown quiesce](2026-09-22-job-archive-shutdown-quiesce.md) owns the job archive behavior kept here.
- [Local Pi configuration](2026-09-19-local-pi-configuration.md) records the Pi resource discovery behavior that the additive extension preserves.
- [digitalygo repository migration](2026-09-19-digitalygo-repository-migration.md) owns the module and repository identity.
- [Spynel architecture security quality gates](../research/2026-09-19-spynel-architecture-security-quality-gates.md) provides the quality and security gate context for the validation below.

## Release and publication status

No commit, push, release, or deployment was requested. The rework is uncommitted on top of `8425af2`, `package.json` still carries the development placeholder, and the live installed service remains the published `v1.6.0`. This record does not claim that any part of the rework is available to installed users.

## References

- [Agent documentation catalog](../../../docs/agent-docs.md)
- [Architecture](../../../docs/architecture.md)
- [Coding harness compatibility](../../../docs/harness-compatibility.md)
- [Communication integrations](../../../docs/integrations.md)
- [Configuration](../../../docs/configuration.md)
- [Extensions](../../../docs/extensions.md)
- [Plain CLI and automation](../../../docs/cli.md)
- [Programmatic integration](../../../docs/programmatic-integration.md)
- [Provider canary threat model](../../../docs/provider-canary-threat-model.md)

## Update 2026-09-25: v2.0.0 publication and local deployment

### Summary of changes

The [Release and publication status](#release-and-publication-status) section above is the pre-publication snapshot and is superseded by this update. The Pi-direct rework was committed as the breaking change `b00efca0600e60b7192fb15e228f827b6496cbcc` (`feat(app)!: make Pi chat direct`), pushed to `main`, and published as [Spynel v2.0.0](https://github.com/digitalygo/spynel/releases/tag/v2.0.0), whose annotated tag object `17683c1f9bc27ae2045c0db6d04edb12a40304b8` peels that commit. The local npm-managed home service was upgraded exactly once from 1.6.0 to 2.0.0 through the official `spynel update` path.

### Technical reasoning

The rework removes two packages and changes provider-visible chat semantics, so it was published as the major release `v2.0.0` rather than a patch. Publication followed the documented two-stage process in [Releasing](../../../docs/releasing.md): a [manual validation run](https://github.com/digitalygo/spynel/actions/runs/36162044479) against the pinned commit with publication skipped, then the annotated tag and the [publishing release run](https://github.com/digitalygo/spynel/actions/runs/36163619933). A unique temporary validation branch pinned the commit, and it was removed after both runs succeeded.

The embedded Telegram Pi note materializes lazily into the private workspace runtime on the first conversation, so no live model call or paid prompt was made during validation, and this update claims no live model compliance. The real Pi fake-provider smoke covers the additive note composition for global and trusted-project `APPEND_SYSTEM.md` discovery instead.

Local deployment used the official coordinated updater exactly once instead of a direct npm replacement. The restarted service kept the same service PID `2090417` but registered a new process generation, which verifies an in-place owner replacement rather than the stale pre-update process reporting a new version.

### Impact assessment

- The public stable `latest` npm package and GitHub Release are now 2.0.0. The Release has five assets: four native archives for Linux and macOS on amd64 and arm64 plus `checksums.txt`. The npm `latest` tag is `@digitalygo/spynel@2.0.0` with SLSA provenance v1.
- The live home-workspace server runs 2.0.0 with the same service PID `2090417` and a new registered generation from `dea13a0e...` to `7646cfd03f85d9b810c8c4ccbecfde0a`, `ready=true`, Telegram and Pi connected, zero jobs, and current and latest both reported as 2.0.0.
- The Telegram Bot API reports the exact ordered 22 command names and descriptions from `internal/app/command_menu.go` in the `all_private_chats` scope. No manual `setMyCommands` call was made, and the new runtime log contains no menu registration failure. This proves provider registration.
- User state was preserved: the private `.spynel` workspace and runtime directories remain mode `0700`, the configuration file remains mode `0600` with unchanged presence and digest, the legacy task, goal, prompt, and instruction metadata digests are unchanged, and no leases exist.
- The Linuxbrew npmrc returned to mode `0444` with its original SHA-256 `a0e43e...` unchanged after the trapped temporary owner-write.
- The embedded Telegram Pi note still has no live compliance evidence: it materializes on the first conversation, and no live model call or paid prompt was made. The real Pi fake-provider run covers composition only.
- The unrelated untracked `.ai-telemetry/` directory was not staged, modified, or packaged, and its contents were not inspected. Repository-wide DOX and smoke gates were run in clean worktrees because that directory produces false findings in the development tree.
- Security note: during a delegated preflight one ephemeral old primary lease token was printed to the private agent transcript. The restart rotated the ownership term and its token per `internal/instance/election.go:158-165`. No Telegram or ElevenLabs secret was printed, and the token value is not recorded here.

### Validation steps

- Clean exact-commit local gates passed in a clean detached worktree: `scripts/dev.sh dox` reported DOX coverage valid for 49 tracked directories, and `scripts/dev.sh test`, `go test -p1 -count1 ./...`, `go vet ./...`, a Go build of `./cmd/spynel`, `scripts/smoke.sh`, and `node npm/test.js` completed without failures.
- Host packaging produced `spynel_2.0.0_linux_amd64.tar.gz` with SHA-256 `bb3c5eed777a720c1687abe6051be7ddaadb25abe0df8e1f2d11a11be6b749e0`, and the extracted binary reported 2.0.0 with the native libraries and licenses present.
- Tracked-source release preparation: `npm/prepare-release.js v2.0.0 false` followed by `npm pack --dry-run` verified the v2.0.0 metadata, tag-pinned README links, a nine-file package, and no telemetry artifacts.
- The real Pi fake-provider smoke passed for global and trusted-project `APPEND_SYSTEM.md`, covering the additive note composition, with no live model call or paid prompt.
- Manual workflow run [36162044479](https://github.com/digitalygo/spynel/actions/runs/36162044479) on the pinned commit passed verify and all four native builds with publish skipped. Its four downloaded native evidence records were independently checked as `observed-native` at source `b00efca0600e60b7192fb15e228f827b6496cbcc` with six passing checks each.
- Published release run [36163619933](https://github.com/digitalygo/spynel/actions/runs/36163619933) on the tag passed verify, all four native builds, and mandatory publish. The annotated tag object `17683c1f9bc27ae2045c0db6d04edb12a40304b8` peels `b00efca0600e60b7192fb15e228f827b6496cbcc`, and the Release lists five assets.
- The public host Linux amd64 archive matched `checksums.txt`, SHA-256 `02d1ef2d29568039bf4d40a5f1b0c9667fe4f8e807a992951caf7ca7fc5e344f`, and the extracted binary reported 2.0.0. An isolated exact `@digitalygo/spynel@2.0.0` npm install passed.
- Local deployment: exactly one official `spynel update` upgraded the npm-managed home service from 1.6.0 to 2.0.0. Afterwards the installed CLI and registered live process reported 2.0.0 with the same PID `2090417`, new generation `7646cfd03f85d9b810c8c4ccbecfde0a`, `ready=true`, Telegram and Pi connected, zero jobs, and current and latest both reported as 2.0.0.
- Workspace state checks: private `.spynel` and runtime directories mode `0700`, configuration mode `0600` with unchanged digest and presence, legacy task, goal, prompt, and instruction metadata digests unchanged, and no leases. The Linuxbrew npmrc was temporarily made owner-writable and restored through a shell trap to mode `0444` with its original SHA-256 `a0e43e...`.
- A credential-safe read-only `getMyCommands` request for `all_private_chats` matched the committed catalog exactly, 22 ordered command names and descriptions. No manual `setMyCommands` call was made, and the new runtime log has no menu registration failure.
- This record carries `updated_at: 2026-09-25`, `docs/releasing.md`, the release URL, and both run URLs in `supporting_docs`. The original narrative, `created_at`, and the 165-path `files_edited` list are unchanged.
